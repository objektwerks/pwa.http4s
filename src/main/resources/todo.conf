server {
  host="0.0.0.0"
  port=7777
}

database {
  schema="/schema.sql"
  driver="org.h2.Driver"
  url="jdbc:h2:~/.todo/tododb;MODE=PostgreSQL;AUTO_RECONNECT=TRUE;DB_CLOSE_DELAY=-1"
  user="sa"
  password="sa"
}